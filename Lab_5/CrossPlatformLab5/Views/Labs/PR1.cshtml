@model CrossPlatformLab5.Models.FirstLabModel

@{
    ViewData["Title"] = "PR1";
}
<h1>@ViewData["Title"]</h1>

<h4>Умова завдання</h4>
<body>
    Требуется найти число способов расставить на шахматной доске N×N K ладей так, чтобы они не били друг друга. Все ладьи считаются одинаковыми.
    <br>Во входном файле INPUT.TXT записаны натуральные числа N и K (N, K ≤ 8).
    <br>В выходной файл OUTPUT.TXT выведите одно целое число – ответ на задачу

</body>

<form method="post" enctype="multipart/form-data" asp-controller="Labs" asp-action="PR1">
    <div class="form-group">
        <label for="labelN">N</label>
        <input type="text" class="form-control" id="labelN" asp-for="N" />
    </div>
    <div class="form-group">
        <label for="labelK">K</label>
        <input type="text" class="form-control" id="labelK" asp-for="K" />
    </div>
    <button type="submit" class="btn btn-primary mt-3">Submit</button>
</form>
@if (Model != null)
{
    @if (Model.Calculated)
    {
        <div class="form-group">
            <label for="response">@Model.Response</label>
        </div>
    }
    @if (!string.IsNullOrEmpty(Model.ErrorValue))
    {
        <div class="form-group">
            <label for="error">@Model.ErrorValue</label>
        </div>
    }
}